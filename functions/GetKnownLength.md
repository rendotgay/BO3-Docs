# GetKnownLength

Get the known length of the sound

Example:
```c++
length = GetKnownLength( level.mySnd )
```

### Parameters
Parameter | Summary
| - | - |
*playbackId | The sound id

<sub>* = mandatory</sub>

