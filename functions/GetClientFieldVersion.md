# GetClientFieldVersion

Returns the version of the specifiec client field, in the specified client field set - or 0 if the field isn't registered.

Example:
```c++
if(GetClientFieldVersion( "world", "fog_volume_active" ))
```


