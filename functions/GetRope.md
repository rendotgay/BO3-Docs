# GetRope

Returns the rope index based on targetname. Use it for ropes setuped in radiant. Returns -1, if not found.

Example:
```c++
GetRope( "testrope" )
```

### Parameters
Parameter | Summary
| - | - |
*targetname | The targetname of the rope you interested in

<sub>* = mandatory</sub>

