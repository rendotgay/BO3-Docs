# SetPauseWorld

pauses playback of animations and effects, but doesn't affect the server tick the way slomo does

Example:
```c++
SetPauseWorld( 1 )
```


### Parameters
Parameter | Summary
| - | - |
*pause | Whether or not to pause the world

<sub>* = mandatory</sub>

