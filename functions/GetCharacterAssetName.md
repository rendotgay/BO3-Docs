# GetCharacterAssetName

Returns the asset name for a character for the current session mode

Example:
```c++
heroName = GetCharacterAssetName( characterIndex, "mp" )
```

### Parameters
Parameter | Summary
| - | - |
*characterIndex | The index of the character in the current game-mode's character table
*sessionmode | Session Mode Index

<sub>* = mandatory</sub>

