# SetMatchTalkFlag

Flags a bit in the match talk state

Example:
```c++
SetMatchTalkFlag( "DeadChatWithDead", "1" )
```

### Parameters
Parameter | Summary
| - | - |
*flag bit | The flag bit to change
*value | The new value of the bit

<sub>* = mandatory</sub>

