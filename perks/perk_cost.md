# Perk Cost
Change how much any perk costs
1. Find your `.gsc` perk script starting with `_zm_perk_` in:
	```
	Call of Duty Black Ops III/share/raw/scripts/zm
	```
2. Find the line that starts with `#define` and has `PERK_COST`
3. Change the number to whatever you like!