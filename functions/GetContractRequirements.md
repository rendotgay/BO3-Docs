# GetContractRequirements

Returns an array of contract requirements. Each requirement takes up two indices, 'reqType' and 'reqData'.

Example:
```c++
mapNames = GetContractRequirements( contractIndex )
```

### Parameters
Parameter | Summary
| - | - |
*contractIndex | The index of the contract

<sub>* = mandatory</sub>

