# PlayerGrappleTrace

Trace to see if grapple is valid

Example:
```c++
hitp = PlayerGrappleTrace(player.origin, player.origin + (0,0,-500))
```

### Parameters
Parameter | Summary
| - | - |
*start | starting position
*end | ending position
[ignore entity] | this entity should be ignored

<sub>* = mandatory</sub>

