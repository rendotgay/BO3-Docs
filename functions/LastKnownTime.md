# LastKnownTime

Return the last time an AI was aware of target. Includes visibility and ai events like footstep, gunshot etc.

Example:
```c++
if( self lastKnownTime( player ) )
```

### Parameters
Parameter | Summary
| - | - |
*target | (sentient) The sentient to check.

<sub>* = mandatory</sub>

