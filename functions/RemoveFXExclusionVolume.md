# RemoveFXExclusionVolume

Removes the given exclusion volume.

Example:
```c++
RemoveFXExclusionVolume( 0, volumeID )
```


### Parameters
Parameter | Summary
| - | - |
*localClientNum | The local client to remove the volume from.
*volumeID | The volume to remove.

<sub>* = mandatory</sub>

