# GetBehaviorTreeStatus

Returns current status/BHTN_STATUS of the behavior tree for an actor.

Example:
```c++
isBehaviorTreeInRunningState = behaviorTreeEntity GetBehaviortreeStatus() == BHTN_RUNNING
```

