# SetViewClamp

Set the view clamp for the player, only works during animscripted for now.  Ranges are relative to tag_camera.  Left, Right, Top, Bottom

Example:
```c++
player SetViewClamp( 50, 50, 30, 30 )
```

