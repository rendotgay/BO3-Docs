# SetupClientFieldAnimSpeedCallbacks

Registers a code callback for each of the given clientfield names in a field pool.

Example:
```c++
SetupClientFieldAnimSpeedCallbacks( "world", 1, "flag_a_owner" )
```


