# IsActorSpawner

Checks whether this entity is an actor spawner

Example:
```c++
if ( IsActorSpawner( ent ) ) ...
```


### Parameters
Parameter | Summary
| - | - |
*entity | An entity object that may be an actor spawner

<sub>* = mandatory</sub>

