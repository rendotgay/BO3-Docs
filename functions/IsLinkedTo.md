# IsLinkedTo

Returns true if the entity is linked to the other entity

Example:
```c++
if (ent IsLinkedTo( other )) { // dostuff }
```

### Parameters
Parameter | Summary
| - | - |
*other | Entity

<sub>* = mandatory</sub>

