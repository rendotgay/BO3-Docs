# IsInMoveMode

Returns true if the player is in either of the given move modes

Example:
```c++
if ( player IsInMoveMode( "ufo", "noclip" ) )
```


### Parameters
Parameter | Summary
| - | - |
*move_mode_1 | The move mode to check
[move_mode_2] | The move mode to check

<sub>* = mandatory</sub>

