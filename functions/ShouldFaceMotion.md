# ShouldFaceMotion

Determines if the AI should face the motion direction. Use self.faceMotion instead of this except when the AI is just beginning to move.

Example:
```c++
self ShouldFaceMotion()
```


