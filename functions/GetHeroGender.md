# GetHeroGender

Returns the given hero's gender ("male" or "female")

Example:
```c++
gender = GetHeroGender( heroIndex, "mp" )
```


### Parameters
Parameter | Summary
| - | - |
*heroIndex | The index of the hero
[sessionmode] | The session mode (cp,mp,zm)

<sub>* = mandatory</sub>

