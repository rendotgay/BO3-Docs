# GetAllCharacterHeads

Returns all character indices, even disabled ones, for the current session mode (or specified session mode)

Example:
```c++
heroIndices = GetAllCharacterHeads()
```

### Parameters
Parameter | Summary
| - | - |
*sessionmode | Session Mode Index

<sub>* = mandatory</sub>

