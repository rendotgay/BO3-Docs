# SetDynEntEnabled

Set this dyn ent to enabled or disabled.  Disabled stops the physics and hides the dyn ent.

Example:
```c++
SetDynEntEnabled( brick, 1 )
```

### Parameters
Parameter | Summary
| - | - |
*dynent | 
[enabled] | 1 if dynent is enabled, 0 otherwise

<sub>* = mandatory</sub>

