# CanClaimNode

Returns true if the node is unclaimed, false otherwise.

Example:
```c++
if ( CanClaimNode( cover, team ) )
```


### Parameters
Parameter | Summary
| - | - |
*node | pathnode to check
*team | 

<sub>* = mandatory</sub>

