# setupclientfieldcodecallbacks

Registers a LUI code callback for each of the given clientfield names in a field pool.

Example:
```c++
SetupClientFieldLuiCodeCallbacks( "world", 1, "flag_a_owner" )
```


