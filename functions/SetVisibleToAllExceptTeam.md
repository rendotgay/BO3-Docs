# SetVisibleToAllExceptTeam

Set self visible to all teams except the selected teams

Example:
```c++
plane SetVisibleToAllExceptTeam( self.team )
```

### Parameters
Parameter | Summary
| - | - |
*team | Which to teams not to show this entity too

<sub>* = mandatory</sub>

