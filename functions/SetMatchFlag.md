# SetMatchFlag

Flags a bit in the match state

Example:
```c++
SetMatchFlag( "bomb_timer", "1" )
```

### Parameters
Parameter | Summary
| - | - |
*flag bit | The flag bit to change
*value | The new value of the bit

<sub>* = mandatory</sub>

