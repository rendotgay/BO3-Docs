# BonusCardActiveCount

Returns how many bonus cards are active, cannot be called on a larry

Example:
```c++
cardCount = self BonusCardActiveCount( self.class_num )
```

### Parameters
Parameter | Summary
| - | - |
*classnum | The current class num of the player

<sub>* = mandatory</sub>

