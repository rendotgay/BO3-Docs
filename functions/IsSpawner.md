# IsSpawner

Checks whether this entity is a spawner

Example:
```c++
if ( IsSpawner( ent ) ) ...
```

### Parameters
Parameter | Summary
| - | - |
*entity | An entity object that may be a spawner

<sub>* = mandatory</sub>

