# CanPlaceRiotshield

Try to place riotshield where the player is standing.  return an array containing placement "result", "origin", and "angles

Example:
```c++
placement = level.player CanPlaceRiotshield()
```

