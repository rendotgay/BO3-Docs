# GetNextArrayKey

Returns the next key of the input array, after the specified key.

Example:
```c++
key = GetNextArrayKey( array, key )
```

### Parameters
Parameter | Summary
| - | - |
*array | Input array
*key | The given key

<sub>* = mandatory</sub>

