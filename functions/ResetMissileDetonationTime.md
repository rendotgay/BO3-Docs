# ResetMissileDetonationTime

Resets the timer for a grenade or missile to its max fuse time.

Example:
```c++
grenadeEnt ResetMissileDetonationTime()
```

### Parameters
Parameter | Summary
| - | - |
[time] | Override the reset timer from the weapon and specify the time left.

<sub>* = mandatory</sub>

