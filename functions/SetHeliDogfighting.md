# SetHeliDogfighting

Sets the vehicle (intended for VTOLs only) to be in dogfighting mode (dogfights with the vehicle's focus entity)

Example:
```c++
vehicle SetHeliDogfighting( true )
```

### Parameters
Parameter | Summary
| - | - |
*enabled | true to enable dogfighting, false otherwise

<sub>* = mandatory</sub>

