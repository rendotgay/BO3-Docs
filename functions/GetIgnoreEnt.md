# GetIgnoreEnt

Returns if a specific sentient entity is ignored by this sentient entity or not

Example:
```c++
if ( aiGuy GetIgnoreEnt( player ) )
```

### Parameters
Parameter | Summary
| - | - |
*sentient | Another sentient entity

<sub>* = mandatory</sub>

