# SetExposureIgnoreTeleport

Always do fully adaptive exposure and ignore teleports

Example:
```c++
SetExposureIgnoreTeleport( localClientNum, newVal )
```


### Parameters
Parameter | Summary
| - | - |
*localClientNum | the local client
*enable | Enable or disable the exposure always adapt flag

<sub>* = mandatory</sub>

