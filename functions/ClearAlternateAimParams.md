# ClearAlternateAimParams

Forces the local player to use default values for aim assist.  Only works in zombie/campaign mode. Can only be called on players

Example:
```c++
self ClearAlternateAimParams()
```


