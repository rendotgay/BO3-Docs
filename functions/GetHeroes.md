# GetHeroes

Returns all hero indices for the current session mode (or specified session mode)

Example:
```c++
heroIndices = GetHeroes()
```

### Parameters
Parameter | Summary
| - | - |
[sessionmode] | Session Mode (cp,mp,zm)

<sub>* = mandatory</sub>

