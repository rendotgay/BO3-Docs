# RotatePoint

Returns &lt;angle&gt; lerped angle value.

Example:
```c++
rotatedPoint = RotatePoint(point, angle )
```

### Parameters
Parameter | Summary
| - | - |
*point | A point.
*angles | An angle.

<sub>* = mandatory</sub>

