# GetMaxVehicles

Return the maximum number of vehicles that can be instantiated simultaneously.  This is platform-specific.

Example:
```c++
vehicleSlotsFree = GetMaxVehicles() - level.vehicles_list.size
```


