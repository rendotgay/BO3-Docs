# GetNodeEdge

Returns an array of points which corresponds to the node volume's traversal edge. The traversal edge is represented as a piecewise linear function.

Example:
```c++
points = GetNodeEdge( myNode )
```


### Parameters
Parameter | Summary
| - | - |
*node | node volume

<sub>* = mandatory</sub>

