# IsCameraSpikeToggled

Returns 1 if the camera spike is toggled to be active, 0 otherwise

Example:
```c++
if ( IsCameraSpikeToggled( 0 ) )
```


### Parameters
Parameter | Summary
| - | - |
*localClientNum | Client number of the camera spike owner

<sub>* = mandatory</sub>

