# SetLocalRadarEnabled

Sets whether a client's local radar is enabled.

Example:
```c++
SetLocalRadarEnabled( localClientNum, 1 )
```


### Parameters
Parameter | Summary
| - | - |
*localClientNum | Client number to set local radar on
*enabled | 0 if disabled, 1 if enabled

<sub>* = mandatory</sub>

