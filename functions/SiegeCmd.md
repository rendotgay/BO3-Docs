# SiegeCmd

Apply a number of anim commands on a siege model

Example:
```c++
thing SiegeCmd( "set_shot", "myshot", "unpause
```

### Parameters
Parameter | Summary
| - | - |
*string cmd | cmd to execute.
[string cmd] | more cmd's...

<sub>* = mandatory</sub>

