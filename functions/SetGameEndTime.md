# SetGameEndTime

Sets the time the current match will end

Example:
```c++
setGameEndTime( getTime() + int(timeLeft) )
```

### Parameters
Parameter | Summary
| - | - |
*time | The time to set

<sub>* = mandatory</sub>

