# GetDefaultClassSlot

Gets the item in a particular default class slot

Example:
```c++
GetDefaultClassSlot( "cqb", "primary" ); // Returns primary weapon reference
```

### Parameters
Parameter | Summary
| - | - |
*classname | the classname of the item
*slotname | The slot to check

<sub>* = mandatory</sub>

