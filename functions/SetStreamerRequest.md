# SetStreamerRequest

Put a streamer hint asset into a request slot (0-7)

Example:
```c++
setStreamerRequest(0, "apple_pie")
```

### Parameters
Parameter | Summary
| - | - |
*slot | Request slot
*name | Name of the streamer hint asset

<sub>* = mandatory</sub>

