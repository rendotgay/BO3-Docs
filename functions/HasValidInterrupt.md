# HasValidInterrupt

Return true if there is a valid interrupt with the given name alive on the current server frame.

Example:
```c++
if ( entity HasValidInterrupt("pain") )
```


### Parameters
Parameter | Summary
| - | - |
*interrupt name | The name of the interrupt

<sub>* = mandatory</sub>

