# OED_SitRepScan_SetDesat

Set the desaturation level for the sitrep scan

Example:
```c++
entity oed_sitrepscan_setdesat( level )
```


### Parameters
Parameter | Summary
| - | - |
*level | the desat level (float)
AlphaType | 0-default 1-Alpha ( 0 by default )

<sub>* = mandatory</sub>

