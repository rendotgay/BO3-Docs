# ResetVisibilityCacheWithinRadius

Reset the viscache for sentients within the specified radius

Example:
```c++
ResetVisibilityCacheWithinRadius(origin, 500)
```

### Parameters
Parameter | Summary
| - | - |
*origin | Center of reset bounds
*radius | Distance around the origin to scan for sentients

<sub>* = mandatory</sub>

