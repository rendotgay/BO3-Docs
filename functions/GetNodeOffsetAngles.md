# GetNodeOffsetAngles

Returns the angles the actor thinks the node is facing, this can contain additional rotation from the node's actual angles.

Example:
```c++
self GetNodeOffsetAngles( self.node )
```

