# SetIgnoreEntForTrigger

Ignores this entity when doing trigger sight traces. Currently only works for trigger_radius_use

Example:
```c++
self SetIgnoreEntForTrigger( camera_spike_ent )
```


### Parameters
Parameter | Summary
| - | - |
*entity to ignore | The entity that will be ignored during sight trace checks

<sub>* = mandatory</sub>

