# GetFlashBangedStrength

Retrieves how strongly the actor was initially hit by a flashbang. 1.0 is full strength, 0.0 is none at all.

Example:
```c++
flashedPercent = self GetFlashBangedStrength()
```


