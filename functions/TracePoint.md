# TracePoint

Traces a poitn throught the world with MASK_SHOT. Returns hit position, hit surface normal.

Example:
```c++
trace = TracePoint( p0, p1 )
```

### Parameters
Parameter | Summary
| - | - |
*start | Start point
*end | End point

<sub>* = mandatory</sub>

