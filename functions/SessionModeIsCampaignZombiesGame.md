# SessionModeIsCampaignZombiesGame

Returns true/false if the current session mode is a campaign game and the campaign mode is set to zombies.

Example:
```c++
if ( SessionModeIsCampaignZombiesGame() )
```


