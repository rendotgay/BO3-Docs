# HasDObj

Returns 1 if the entity currently has a dobj.

Example:
```c++
if(ent HasDObj(0))
```

### Parameters
Parameter | Summary
| - | - |
*local client number | number of the local client on the machine.

<sub>* = mandatory</sub>

