# GetFlowGraphDef

returns a struct representing the flow graph definition asset

Example:
```c++
flowGraphDef = GetFlowGraphDef( "my_flow_graph" )
```

### Parameters
Parameter | Summary
| - | - |
*name | The name of the flow graph asset

<sub>* = mandatory</sub>

