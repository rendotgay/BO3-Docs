# OED_SitRepScan_SetFalloff

Set the falloff for the sitrep scan

Example:
```c++
entity oed_sitrepscan_setfalloff( falloff )
```

### Parameters
Parameter | Summary
| - | - |
*falloff | The falloff (float)
AlphaType | 0-default 1-Alpha ( 0 by default )

<sub>* = mandatory</sub>

