# LastKnownPos

Return the last known pos of an AI.

Example:
```c++
if ( self lastKnownPos( player ) )
```


### Parameters
Parameter | Summary
| - | - |
*target | (sentient) The sentient to check.

<sub>* = mandatory</sub>

