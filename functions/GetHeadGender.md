# GetHeadGender

Returns the given head's gender ("male" or "female")

Example:
```c++
gender = GetHeadGender( headIndex, "mp" )
```


### Parameters
Parameter | Summary
| - | - |
*headIndex | The index of the head
[sessionmode] | The session mode (cp,mp,zm)

<sub>* = mandatory</sub>

