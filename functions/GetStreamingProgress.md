# GetStreamingProgress

Gets a progress value between 0 (no progress) and 1 (complete) for all the models and meshes that the script is currently forcing

Example:
```c++
progress = GetStreamingProgress()
```

