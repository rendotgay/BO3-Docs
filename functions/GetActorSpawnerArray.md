# GetActorSpawnerArray

Returns an array of all of the actor spawners in a level.

Example:
```c++
spawners = GetSpawnerArray()
```


### Parameters
Parameter | Summary
| - | - |
[name] | Name to search for.
[key] | The key to search for.

<sub>* = mandatory</sub>

