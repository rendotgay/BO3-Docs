# Sqrt

Returns the square-root of floating point value &lt;a&gt;.

Example:
```c++
c = sqrt( (a*a) + (b*b) )
```


### Parameters
Parameter | Summary
| - | - |
*a | A float or integer value.

<sub>* = mandatory</sub>

